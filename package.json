{
  "name": "slack-app",
  "version": "1.0.0",
  "description": "Send markdown as Slack blocks via HTTP (user token, no Socket Mode)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node src/test-incoming-markdown.js"
  },
  "keywords": [
    "slack",
    "bot",
    "bolt"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@slack/web-api": "^6.13.0",
    "@tryfabric/mack": "^1.2.1",
    "dotenv": "^16.4.5",
    "express": "^5.2.1"
  }
}
